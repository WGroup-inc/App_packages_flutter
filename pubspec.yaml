name: wedoo_sdk
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  app_core:
    path: packages/app_core
  onboarding:
    path: packages/onboarding
  signed_in:
    path: packages/signed_in

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
